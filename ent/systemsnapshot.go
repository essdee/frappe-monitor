// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"frappe-monitor/ent/server"
	"frappe-monitor/ent/systemsnapshot"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// SystemSnapshot is the model entity for the SystemSnapshot schema.
type SystemSnapshot struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CapturedAt holds the value of the "captured_at" field.
	CapturedAt time.Time `json:"captured_at,omitempty"`
	// Payload holds the value of the "payload" field.
	Payload []byte `json:"payload,omitempty"`
	// LastError holds the value of the "last_error" field.
	LastError string `json:"last_error,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SystemSnapshotQuery when eager-loading is set.
	Edges                  SystemSnapshotEdges `json:"edges"`
	server_system_snapshot *int
	selectValues           sql.SelectValues
}

// SystemSnapshotEdges holds the relations/edges for other nodes in the graph.
type SystemSnapshotEdges struct {
	// Server holds the value of the server edge.
	Server *Server `json:"server,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ServerOrErr returns the Server value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SystemSnapshotEdges) ServerOrErr() (*Server, error) {
	if e.Server != nil {
		return e.Server, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: server.Label}
	}
	return nil, &NotLoadedError{edge: "server"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SystemSnapshot) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case systemsnapshot.FieldPayload:
			values[i] = new([]byte)
		case systemsnapshot.FieldID:
			values[i] = new(sql.NullInt64)
		case systemsnapshot.FieldLastError:
			values[i] = new(sql.NullString)
		case systemsnapshot.FieldCapturedAt:
			values[i] = new(sql.NullTime)
		case systemsnapshot.ForeignKeys[0]: // server_system_snapshot
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SystemSnapshot fields.
func (_m *SystemSnapshot) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case systemsnapshot.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case systemsnapshot.FieldCapturedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field captured_at", values[i])
			} else if value.Valid {
				_m.CapturedAt = value.Time
			}
		case systemsnapshot.FieldPayload:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field payload", values[i])
			} else if value != nil {
				_m.Payload = *value
			}
		case systemsnapshot.FieldLastError:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field last_error", values[i])
			} else if value.Valid {
				_m.LastError = value.String
			}
		case systemsnapshot.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field server_system_snapshot", value)
			} else if value.Valid {
				_m.server_system_snapshot = new(int)
				*_m.server_system_snapshot = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SystemSnapshot.
// This includes values selected through modifiers, order, etc.
func (_m *SystemSnapshot) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryServer queries the "server" edge of the SystemSnapshot entity.
func (_m *SystemSnapshot) QueryServer() *ServerQuery {
	return NewSystemSnapshotClient(_m.config).QueryServer(_m)
}

// Update returns a builder for updating this SystemSnapshot.
// Note that you need to call SystemSnapshot.Unwrap() before calling this method if this SystemSnapshot
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *SystemSnapshot) Update() *SystemSnapshotUpdateOne {
	return NewSystemSnapshotClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the SystemSnapshot entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *SystemSnapshot) Unwrap() *SystemSnapshot {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: SystemSnapshot is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *SystemSnapshot) String() string {
	var builder strings.Builder
	builder.WriteString("SystemSnapshot(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("captured_at=")
	builder.WriteString(_m.CapturedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("payload=")
	builder.WriteString(fmt.Sprintf("%v", _m.Payload))
	builder.WriteString(", ")
	builder.WriteString("last_error=")
	builder.WriteString(_m.LastError)
	builder.WriteByte(')')
	return builder.String()
}

// SystemSnapshots is a parsable slice of SystemSnapshot.
type SystemSnapshots []*SystemSnapshot
