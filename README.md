# frappe-monitor

Monitoring and alerting for multi-server, multi-bench, multi-site Frappe deployments.

See `docs/` for the master design plan and phase plans.

## Run (Phase 1)

```bash
cp deploy/config/monitor.yaml.example config/monitor.yaml
make run
```
